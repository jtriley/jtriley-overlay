# /etc/conf.d/g15daemon: Configuration for the g15daemon

# Key to switch the client-screens. Default is the L1 key,
# Set to "yes" to use macro record (MR) key instead.
CLIENT_SWITCH_MR="no"

# Set to "yes" to switch off the lcd backlight when stopping g15daemon.
BACKLIGHT_OFF="no"
